#include "Grammar.h"

Grammar::Grammar(/*set<char> nonterminals, set<char> terminals, char start, set<pair<string, string>> rules) : nonterminals(nonterminals)*/)
{
}

string Grammar::hello() const
{
    return "hello";
}
